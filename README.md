# Crypto Monitor

Сервис по поиску потенциально выгодных арбитражных сделок между криптобиржами. Построен по архитектуре Ports & Adapters, имеет 3 микросервиса, связанных по REST API, с расчетом на удобную миграцию на gRPC.

Роли микросервисов:

- `fetcher` собирает котировки с Binance, Bybit, Coinbase и OKX
- `analyzer` пересчитывает спред с учетом реальных комиссий
- `executor` TODO

## Как это работает

1. `fetcher` с заданной частотой запрашивает цены по отслеживаемым монетам и отправляет их в `analyzer`.
2. `analyzer` принимает цены, хранит их в кеше по монетам и биржам, пересчитывает реальную цену покупки и продажи с учетом комиссий и ищет положительный спред.
3. Если найден профит, `analyzer` отправляет сделку в `executor`. `executor` сейчас только выводит результат в лог.

## Запуск

* `make build` - сборка бинарников
* `make run-<Имя сервиса>` - запуск
* `make up` - поднятие окружения через Docker Compose

## Перспективные введения для ускорения обработки

* [ ] Контроль свежести через context / timestamp
* [ ] Внедрение sync.Pool
* [ ] Внедрение WebSocket
* [ ] Внедрение Redis
* [ ] Переход на gRPC

---

*Проект в процессе разработки*
